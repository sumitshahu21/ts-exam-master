import { Router } from 'express';

const router = Router();

// Placeholder routes for tests
router.get('/', (req, res) => {
  res.json({ message: 'Test routes - Coming soon' });
});

export default router;
